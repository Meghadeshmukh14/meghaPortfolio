## meghaPortfolio

Personal portfolio website built with React.

### Tech stack

- React + React Router
- Bootstrap

### Getting started

Install dependencies:

```bash
npm install
```

Run locally:

```bash
npm start
```

Build for production:

```bash
npm run build
```

### Notes

- The app uses a versioned router (`/v2` is the active version).
